{
  "name": "@cf-office-hours/api",
  "version": "0.2.0",
  "license": "MIT OR Apache-2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "esbuild src/index.ts --bundle --format=esm --outfile=dist/index.js --loader:.sql=text",
    "deploy": "wrangler deploy",
    "deploy:staging": "wrangler deploy --env staging",
    "deploy:production": "wrangler deploy --env production",
    "test": "vitest run",
    "test:watch": "vitest",
    "tail": "wrangler tail",
    "lint": "eslint src --max-warnings 0",
    "type-check": "tsc --noEmit",
    "populate-match-cache": "tsx src/scripts/populate-match-cache.ts"
  },
  "dependencies": {
    "@cf-office-hours/shared": "*",
    "@hono/swagger-ui": "^0.6.1",
    "@hono/zod-openapi": "^1.4.0",
    "date-fns": "^4.4.0",
    "hono": "^4.12.27",
    "jose": "^6.1.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@cf-office-hours/config": "*",
    "@cloudflare/workers-types": "^4.20260629.1",
    "esbuild": "^0.28.1",
    "tsx": "^4.19.2",
    "vitest": "^4.0.0",
    "wrangler": "4.102.0"
  }
}
